{
  "name": "alumni-networking-portal",
  "version": "1.0.0",
  "description": "A comprehensive MERN alumni networking portal",
  "main": "server/server.js",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
"install-all":"npm install && cd server && npm install && cd ../client && npm install","start":"npm run dev"
  },
  "keywords": ["alumni", "networking", "mern", "portal"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
